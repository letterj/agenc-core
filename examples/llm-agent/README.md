# LLM Agent Example

Internal runtime-dependent example for LLM-backed agent execution.

Local commands:

```bash
npm --prefix examples/llm-agent run typecheck
npm --prefix examples/llm-agent run start
```

Use this workspace when changing model-provider or chat-execution flows in the
runtime.

