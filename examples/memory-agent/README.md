# Memory Agent Example

Internal runtime-dependent example for memory-backed agent behavior.

Local commands:

```bash
npm --prefix examples/memory-agent run typecheck
npm --prefix examples/memory-agent run start
```

