# Autonomous Agent Example

Internal runtime-dependent example for autonomous agent execution.

This example is validated through TypeScript workspace contracts rather than by
running the full runtime flow in CI.

Local commands:

```bash
npm --prefix examples/autonomous-agent run typecheck
npm --prefix examples/autonomous-agent run start
```

